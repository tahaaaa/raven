raven {
  prd = false //if set to false, only recipients matching 'restrict-to' regex will be sent
  restrict-to = "^(aaron|acacia|adam|alberto|alexis|amac|amelia|andres|andresm|andrew|ankur|ardy|ari|armando|awroblicky|badri|bill|brg|brian|calvin|carolin|cesar|charley|chema|chetan|cjc|craig|crdlc|david|davidhoatson|delia|dimitri|dmun|eanicich|eleanor|emily|eric|ernest|garret|ggb|gian|guillermo|hashir|ian|ibai|icfb|jaideep|james|jaoo|javierc|javierdiaz|jchang|jneiman|joey|jonathan|jpujol|jtsai|julie|justin|jwade|karthik|kfox|konstancia|lakshmanan|laura|lauren|lawrence|lee|leigh|lhayes|liz|lucas|madhav|mads|manas|marinas|mavila|meeta|melih|mike|mleaney|msander|mwilson|nan|oscar|paulr|payam|philipp_old|pqg|puneet|rclifford|robert|roland|rolly|rpc|ryan|sara|scott|skteh|sridhar|stan|steve|swartz|tedwards|tessa|tiffany|tommy|utkarsh|venkat|kyle|taha|trash)(\\+(\\w[-.]*)*)?@tokbox.com$"
  host = "0.0.0.0"
  port = 9911
  sendgrid {
    api-key = ""
  }
  max-retries = 3
  deferrer = 2 //seconds to wait until next try in case one of ther requests fails
  certified-pool = 1 //parallelization certified service
  priority-pool = 10 //parallelization of priority service
  actor-timeout = 4s //timeout applied to couriers talking to dao/email provider
  endpoint-timeout = 30s //max-retries * deferrer * actor-timeout should be smaller than this

  database = {
    connectionTestQuery = "select 1;"
    dataSourceClass = "com.mysql.jdbc.jdbc2.optional.MysqlDataSource"
    properties = {
      serverName = "localhost"
      portNumber = "3306"
      databaseName = "raven"
      user = "root"
      password = "root"
    }
  }
}

akka {

  loggers = ["akka.event.slf4j.Slf4jLogger"]
  logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"

  # Log level used by the configured loggers (see "loggers") as soon
  # as they have been started; before that, see "stdout-loglevel"
  # Options: OFF, ERROR, WARNING, INFO, DEBUG
  loglevel = "DEBUG"

  # Log level for the very basic logger activated during ActorSystem startup.
  # This logger prints the log messages to stdout (System.out).
  # Options: OFF, ERROR, WARNING, INFO, DEBUG
  stdout-loglevel = "INFO"


  actor {
    priority-dispatcher {
      # Dispatcher is the name of the event-based dispatcher
      type = Dispatcher
      # What kind of ExecutionService to use
      executor = "fork-join-executor"
      # Configuration for the fork join pool
      fork-join-executor {
        # Min number of threads to cap factor-based parallelism number to
        parallelism-min = 4
        # Parallelism (threads) ... ceil(available processors * factor)
        parallelism-factor = 4.0
        # Max number of threads to cap factor-based parallelism number to
        parallelism-max = 20
      }
      # Throughput defines the maximum number of messages to be
      # processed per actor before the thread jumps to the next actor.
      # Set to 1 for as fair as possible.
      throughput = 100
    }

    deployment {
      /SMTPService {
        router = round-robin-pool
        nr-of-instances = 5 //max number of parallel calls to sendgrid SMTP API
      }
    }
  }
}
